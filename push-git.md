
---

# 🚀 服务器代码推送到 GitHub 操作指南

本文档用于指导如何将 Linux 服务器上的现有项目（如 `GIBH-AGENT`）完整同步到 GitHub 远程仓库，同时避免上传大模型文件和敏感数据。

## ✅ 前置准备

1.  拥有一个 GitHub 账号。
2.  拥有服务器的终端访问权限。
3.  项目文件夹已存在于服务器（例如 `~/GIBH-AGENT`）。

---

## 第一步：在 GitHub 创建空仓库

1.  登录 GitHub，点击右上角 **+** -> **New repository**。
2.  **Repository name**: 输入项目名（如 `GIBH-AGENT`）。
3.  **Description**: (可选) 填写项目描述。
4.  **Public/Private**: 根据需求选择。
5.  **⚠️ 关键点**：**不要**勾选 "Add a README file"、".gitignore" 或 "License"。（保持仓库完全空白，可以减少后续合并冲突）。
6.  点击 **Create repository**。
7.  复制仓库的 **SSH 地址**（例如：`git@github.com:username/repo-name.git`）。

---

## 第二步：配置忽略文件 (至关重要)

在服务器项目根目录下，配置 `.gitignore` 以防止上传模型权重（GB级）、数据库文件和临时文件。

```bash
cd ~/GIBH-AGENT
nano .gitignore
```

**粘贴以下内容：**

```text
# === 核心数据与模型 (绝对不能传) ===
data/models/
data/uploads/
data/redis/
data/postgres/
data/qdrant/
data/logs/

# === Python 缓存与环境 ===
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/

# === IDE 配置 ===
.idea/
.vscode/
.DS_Store

# === 敏感配置 ===
.env
```
*保存退出：`Ctrl+O` -> `Enter` -> `Ctrl+X`*

---

## 第三步：配置 SSH 密钥 (免密上传)

GitHub 不再支持密码推送，必须使用 SSH 密钥。

1.  **检查是否已有密钥**：
    ```bash
    cat ~/.ssh/id_ed25519.pub
    ```
    *   如果有输出乱码字符串，直接复制。
    *   如果提示 `No such file`，执行生成命令：
        ```bash
        ssh-keygen -t ed25519 -C "server_git_key"
        # 一路回车即可
        cat ~/.ssh/id_ed25519.pub
        ```

2.  **添加到 GitHub**：
    *   GitHub 网页 -> 右上角头像 -> **Settings**。
    *   左侧菜单 -> **SSH and GPG keys** -> **New SSH key**。
    *   **Title**: `GPU-Server`。
    *   **Key**: 粘贴刚才复制的公钥内容。
    *   点击 **Add SSH key**。

3.  **测试连接**：
    ```bash
    ssh -T git@github.com
    # 输入 yes，看到 "Hi username! You've successfully authenticated..." 即成功
    ```

---

## 第四步：初始化与推送 (核心操作)

回到服务器终端，在项目根目录下执行：

### 场景 A：GitHub 仓库是空的 (最简单)

```bash
# 1. 初始化 Git
git init

# 2. 配置提交人信息
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"

# 3. 添加文件 (会自动读取 .gitignore)
git add .

# 4. 提交到本地
git commit -m "Initial commit: Project setup"

# 5. 切换分支名为 main (GitHub 默认标准)
git branch -m main

# 6. 关联远程仓库 (替换为你自己的 SSH 地址)
git remote add origin git@github.com:你的用户名/GIBH-AGENT.git

# 7. 推送
git push -u origin main
```

---

### 场景 B：GitHub 仓库里已经有文件了 (如 README/License)

如果你创建仓库时手滑勾选了 README，直接推会报错。需要先合并。

```bash
# 1-6 步同上...

# 7. 先拉取远程文件并允许不相关历史合并
git pull origin main --allow-unrelated-histories
# (如果弹出编辑器，直接 Ctrl+O, Enter, Ctrl+X 保存默认合并信息)

# 8. 再次推送
git push -u origin main
```

---

## 🔧 常用维护指令

### 1. 修改代码后更新到 GitHub
```bash
git add .
git commit -m "更新了: 修复了前端流式输出的Bug"
git push
```

### 2. 强制覆盖远程 (慎用)
如果远程仓库乱了，你想完全以服务器本地为准：
```bash
git push -u origin main --force
```

### 3. 检查当前状态
```bash
git status
```
*(这能看到哪些文件被修改了，哪些文件被忽略了)*
